# SteamToolPlus项目代码深度优化审计指令（专业完整版）

请对**SteamToolPlus完整项目**进行全方位、细致化的全局代码审查与性能优化梳理，严格执行指定目录/文件排除规则、性能指标标准、算法优化规范，全程仅输出优化清单，**禁止修改、删除、新增项目原有代码**，仅做审计分析与优化方案罗列，具体执行要求如下：

## 一、项目排除范围（全程忽略，不审计、不优化、不纳入清单）

本次优化工作**完全排除**项目内以下文件夹、脚本及工具模块，不对其进行任何代码核查与性能优化分析：

games\-config\-editor、OpenSteamTool\-1\.4\.8、successfully\-tested\-editor、release\-tool

## 二、核心性能优化硬性指标

本次所有优化方案，最终需实现程序运行性能大幅提升，确保软件**运行时内存占用、CPU占用**均降至原版本的**60%及以下**，以低资源消耗、高运行效率为核心优化目标。

## 三、算法与代码编写优化规范（强制优先执行）

梳理代码问题、制定优化方案时，严格遵循高性能编码原则，全面重构低效逻辑：

1\. **优先采用高速高效算法**：摒弃老旧、低效、冗余的基础逻辑，替换为时间复杂度、空间复杂度更低的高效算法，从底层提升程序运行效率；

2\. **极致精简循环逻辑**：最大限度减少for、while、foreach等各类循环结构，杜绝多层嵌套循环、无效空循环、重复遍历循环，对必须保留的循环进行逻辑合并、次数精简、预加载优化；

3\. **杜绝多层条件嵌套**：大幅简化if\-else、if\-else if嵌套结构，消除深度层级条件判断，通过查表法、枚举匹配、短路逻辑、三元运算、哈希映射等方式替代复杂嵌套条件，扁平化代码逻辑；

4\. **清理冗余无效代码**：排查并统计无效变量定义、重复赋值、闲置函数、未执行分支、冗余判断、重复计算等问题，纳入优化清单；

5\. **优化资源调度逻辑**：针对内存常驻、资源未释放、CPU轮询冗余、重复加载、缓存失效等性能痛点，制定对应的精细化优化方案。

## 四、输出要求（详细优化清单）

最终输出一份**结构化、精细化、可落地的完整代码优化清单**，清单需清晰标注：问题代码位置、原有低效逻辑、性能损耗原因、对应优化方案、采用的高速算法、优化后性能提升预期、内存/CPU优化增益说明，条理清晰、分类明确、无遗漏、无模糊表述，完全适配后续代码迭代优化使用。

> (Note: May contain AI-generated content.)
